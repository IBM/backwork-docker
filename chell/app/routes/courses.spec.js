describe('courses', () => {
  describe('/', () => {
    describe('get', () => {
      it('should list all courses');
    });
  });

  describe('/:courseId', () => {
    describe('get', () => {
      it('should retrieve the specified course');
    });
  });
});
